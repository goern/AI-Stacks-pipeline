kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "ai-stacks-pipeline"
spec:
  source:
    git:
      uri: "https://github.com/AICoE/AI-Stacks-pipeline"
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfilePath: Jenkinsfile
